<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appfood">

    <application
        android:name=".classs.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_hankha"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_hankha_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFood">
        <activity android:name=".OnboardingActivity" android:theme="@style/Theme.AppFood.No"></activity>
        <activity android:name=".HistoryActivity" />
        <activity android:name=".ViewAllActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".SearchActivity" />
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/Theme.AppFood.No">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FoodActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppFood">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>